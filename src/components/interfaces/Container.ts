/**
 * The Container interface. A container wraps arbitrary pieces of functionality.
 */
export interface Container {

  /**
   * Returns the stringified uuid of the container.
   */
  getId: string;
}
